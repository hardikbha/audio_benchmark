# Audio Benchmark Requirements
# Core dependencies for running the audio benchmark pipeline

# LLM Clients
openai>=1.0.0                  # OpenAI API and vLLM client
anthropic>=0.15.0              # Anthropic Claude client
google-generativeai>=0.3.0     # Google Gemini client

# Deep Learning
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.40.0
accelerate>=0.25.0
safetensors>=0.4.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
scipy>=1.10.0
numpy>=1.24.0

# Web Framework (for tool server)
fastapi>=0.100.0
uvicorn>=0.23.0
requests>=2.31.0
pydantic>=2.0.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# ============================================
# AUDIO TOOLS (install as needed)
# ============================================

# Whisper (transcription)
faster-whisper>=0.9.0
# OR: openai-whisper>=20230918

# NISQA (quality assessment)
# pip install git+https://github.com/gabrielmittag/NISQA.git

# Demucs (source separation)
demucs>=4.0.0

# DeepFilterNet (enhancement)
# pip install deepfilternet

# SpeechBrain (speaker verification, language ID)
speechbrain>=0.5.0

# PyAnnote (diarization)
# pyannote-audio>=3.0  # Requires HuggingFace token

# Silero (VAD)
# Install via torch.hub

# AudioSeal (watermarking)
# pip install audioseal

# ============================================
# OPTIONAL: Quantization (for local models)
# ============================================
# auto-gptq>=0.5.0    # Requires CUDA
# optimum>=1.27.0
# bitsandbytes>=0.41.0
